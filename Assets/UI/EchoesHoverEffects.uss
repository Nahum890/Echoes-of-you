/* ============================================================
   ECHOES OF YOU — SISTEMA DE HOVER ICÓNICO (FASE 6 CANÓN)
   PS2 / VHS / CRT / Memoria Analógica — SOLO 4 CAPAS
   
   CAPAS PERMITIDAS:
   1. BASE — .nav-item-v2
   2. HOVER/FOCUS — .nav-item-v2:hover, .nav-item-v2:focus
   3. ACTIVE — .nav-item-v2:active
   4. GHOST (1 SOLO) — .nav-item-ghost
   
   FOCUS PIP (controller) — .nav-item-focus-pip--visible
   
   ELIMINADOS COMPLETAMENTE:
   - .nav-item-chroma-r / .nav-item-chroma-b (aberración cromática)
   - .nav-item-noise (capa de ruido)
   - .nav-item-scanline (scanline)
   - .nav-item-echo (eco visual)
   - .nav-item-confirm-flash (flash confirmación)
   - .nav-item-ghost-a / .nav-item-ghost-b (2 ghosts → 1 solo)
   - .panel-tint-* (tints panel derecho)
   - .side-nav-gradient-* (gradientes)
   - .ui-particle / .ui-particle--* (partículas)
   - .right-panel-crt-blink (CRT blink)
   
   TIMING VARIABLES (desde EchoesDesignTokens.uss):
   --duration-hover-mouse: 150ms
   --duration-hover-controller: 200ms
   --duration-active: 80ms
   ============================================================ */

@import "EchoesDesignTokens.uss";

/* ============================================================
   WRAPPER — Contenedor relativo para posicionar ghost layer
   ============================================================ */

.nav-item-wrapper {
    position: relative;
    overflow: visible;
    margin-bottom: 8px;
}

/* ============================================================
   1. ESTADO BASE — .nav-item-v2
   ============================================================ */

.nav-item-v2 {
    /* Layout */
    position: relative;
    padding: 16px 20px 16px 22px;
    overflow: visible;

    /* Apariencia base */
    background-color: transparent;
    border-width: 0;
    border-left-width: 0;
    border-left-color: rgba(255, 191, 0, 0);

    /* Tipografía */
    -unity-text-align: middle-left;
    font-size: 20px;
    letter-spacing: 1.5px;
    -unity-font-definition: url('project://database/Assets/UI/Fonts/CourierBold.ttf');
    -unity-text-transform: uppercase;
    color: rgba(45, 42, 38, 0.92);

    /* Transform — origen en borde izquierdo para que escale hacia la derecha */
    transform-origin: left center;
    translate: 0px 0px;
    scale: 1.0 1.0;

    /* Transiciones base */
    transition-property: color, translate, scale, border-left-width, border-left-color, background-color, opacity;
    transition-duration: var(--duration-hover-mouse), var(--duration-hover-mouse), var(--duration-hover-mouse), var(--duration-hover-mouse), var(--duration-hover-mouse), var(--duration-hover-mouse), var(--duration-hover-mouse);
    transition-timing-function:
        ease-out,
        cubic-bezier(0.25, 0.46, 0.45, 0.94),
        cubic-bezier(0.34, 1.56, 0.64, 1),
        ease-out,
        ease-out,
        ease-out,
        ease-out;
    transition-delay: 0ms, 0ms, 0ms, 0ms, 0ms, 0ms, 0ms;
}

/* ============================================================
   2. ESTADO HOVER/FOCUS
   Mouse: 150ms | Controller: 200ms (variables CSS)
   ============================================================ */

.nav-item-v2:hover,
.nav-item-v2:focus {
    /* Color del texto — transición inmediata */
    color: rgba(59, 90, 110, 1.0);

    /* Movimiento: desliza 6px a la derecha */
    translate: 6px 0px;

    /* Escala con overshoot: cubic-bezier(0.34, 1.56, 0.64, 1) */
    scale: 1.035 1.035;

    /* Línea de profundidad izquierda — aparece al hover */
    border-left-width: 2px;
    border-left-color: rgba(255, 191, 0, 0.9);

    /* Fondo sutil para separar del fondo */
    background-color: rgba(59, 90, 110, 0.04);
}

/* Focus pip (controller) — indicador ▸ a la izquierda */
.nav-item-focus-pip {
    position: absolute;
    left: 4px;
    top: 0;
    bottom: 0;
    width: 8px;
    -unity-text-align: middle-center;
    font-size: 10px;
    color: rgba(255, 191, 0, 0.0);
    pointer-events: none;
    transition-property: color, translate;
    transition-duration: var(--duration-hover-controller), var(--duration-hover-controller);
    transition-timing-function: ease-out, ease-out;
}

.nav-item-focus-pip--visible {
    color: rgba(255, 191, 0, 0.85);
}

/* ============================================================
   3. ESTADO ACTIVE (PRESSED)
   Duración 80ms
   ============================================================ */

.nav-item-v2:active {
    scale: 0.97 0.97;
    translate: 2px 0px;
    color: rgba(255, 191, 0, 1.0);
    background-color: rgba(255, 191, 0, 0.08);
    border-left-color: rgba(255, 191, 0, 1.0);
    border-left-width: 3px;
    transition-duration: var(--duration-active), var(--duration-active), var(--duration-active), var(--duration-active), var(--duration-active), var(--duration-active), var(--duration-active);
    transition-timing-function:
        ease-in,
        cubic-bezier(0.68, -0.55, 0.27, 1.55),
        cubic-bezier(0.68, -0.55, 0.27, 1.55),
        ease-in,
        ease-in,
        ease-in,
        ease-in;
}

/* ============================================================
   4. ESTADO ACTIVO / SELECCIONADO (nav-item--active)
   ============================================================ */

.nav-item-v2.nav-item--active {
    color: rgba(255, 191, 0, 1.0);
    border-left-width: 2px;
    border-left-color: rgba(255, 191, 0, 1.0);
    background-color: rgba(255, 191, 0, 0.06);
    translate: 4px 0px;
    scale: 1.02 1.02;
}

/* ============================================================
   GHOST LAYER (1 SOLO) — offset 4px, opacity 0.12, delay 50ms
   Representa la señal que "llegó antes" — una memoria anterior
   ============================================================ */

.nav-item-ghost {
    position: absolute;
    left: -4px;
    top: 0px;
    right: -4px;
    bottom: 0px;

    /* Misma tipografía que el botón pero transparente en idle */
    -unity-text-align: middle-left;
    font-size: 20px;
    letter-spacing: 1.5px;
    -unity-font-definition: url('project://database/Assets/UI/Fonts/CourierBold.ttf');
    -unity-text-transform: uppercase;
    color: rgba(59, 90, 110, 0.0);
    padding: 16px 20px 16px 26px;

    /* No debe capturar eventos */
    pointer-events: none;

    /* Transición con delay para aparecer después del botón */
    transition-property: color, opacity;
    transition-duration: 180ms, 180ms;
    transition-timing-function: ease-out, ease-out;
    transition-delay: 50ms, 50ms;
}

/* Ghost activado por clase CSS aplicada via C# */
.nav-item-ghost--visible {
    color: rgba(59, 90, 110, 0.12);
}

/* ============================================================
   TIMING VARIABLES (definidas en EchoesDesignTokens.uss, replicadas aquí para reference)
   ============================================================ */

:root {
    --duration-hover-mouse: 150ms;
    --duration-hover-controller: 200ms;
    --duration-active: 80ms;
}