schema_version: "1.0"
rule_set: composition
rules:
  - id: COMP-001
    type: validation
    status: active
    severity: error
    scope: [composition, level]
    rule: "Every level must declare at least one composition profile for its core puzzle"
    validator: validators/blueprint_validator.yaml#composition_profile
